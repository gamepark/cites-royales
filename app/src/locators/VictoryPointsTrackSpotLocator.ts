import { Locator } from '@gamepark/react-game'

class VictoryPointsTrackSpotLocator extends Locator {
  coordinates = { x: -40, y: -9 }
}

export const victoryPointsTrackSpotLocator = new VictoryPointsTrackSpotLocator()
